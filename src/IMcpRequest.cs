namespace McpFramework
{
    /// <summary>
    /// Marker interface for MCP request models that should be validated
    /// by the McpValidationFilter when [McpTool] attribute is present
    /// </summary>
    public interface IMcpRequest
    {
    }
}
